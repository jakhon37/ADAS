[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "adas-core"
version = "0.2.0"
description = "ADAS reference pipeline (ACC, LKA, tracking, safety) with optional TensorRT perception on Jetson"
readme = "README.md"
requires-python = ">=3.8"
authors = [{ name = "Jakhongir Nodirov", email = "jakhon37@gmail.com" }]
license = { text = "MIT" }
keywords = ["adas", "autonomous-driving", "ros2", "jetson", "computer-vision", "acc", "lane-keeping"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

[project.urls]
Homepage = "https://github.com/jakhon37/ADAS"
Documentation = "https://github.com/jakhon37/ADAS#readme"
Repository = "https://github.com/jakhon37/ADAS"
Issues = "https://github.com/jakhon37/ADAS/issues"
Changelog = "https://github.com/jakhon37/ADAS/blob/main/CHANGELOG.md"

[project.optional-dependencies]
dev = [
  "pytest>=7.4,<8.4",
  "ruff>=0.3",
  "pytest-cov>=4.0",
]
ros2 = [
  "rclpy>=3.0",
]
jetson = []
all = [
  "pytest>=7.4,<8.4",
  "ruff>=0.3",
  "pytest-cov>=4.0",
  "rclpy>=3.0",
]

[project.scripts]
adas-run = "adas.cli:main"
adas-bridge = "adas.ros2.bridge:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
addopts = "-q"
cache_dir = "/tmp/adas-pytest-cache"

[tool.ruff]
line-length = 100
src = ["src", "tests"]
